CREATE DATABASE biblioteca;

use biblioteca;

create table livro(
idlivro int primary key auto_increment,
titulo varchar(255),
qtdepaginas int,
genero varchar(255),
autor varchar(255),
errei varchar(255)
);

use biblioteca;

alter table livro add column ano int;
alter table livro drop column errei;

drop database biblioteca;